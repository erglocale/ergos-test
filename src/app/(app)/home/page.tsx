"use client";

import FleetAndChargerHostLayout from "@/components/home/FleetAndChargerHostLayout";

// Mirrors ergOS-frontend/src/pages/Home/index.jsx — the dashboard renders the
// fleet overview + live map split layout.
export default function Home() {
  return <FleetAndChargerHostLayout />;
}
